{
  "owner_ids": {
    "description": "允许使用插件的用户 ID 列表",
    "type": "list",
    "default": [],
    "hint": "留空时插件不会读取活动或生成日记。"
  },
  "allow_group_commands": {
    "description": "群聊中允许使用的命令列表",
    "type": "list",
    "default": ["日记状态"],
    "hint": "仅允许无敏感摘要的命令；正文、测试、补写和重写始终只允许私聊。"
  },
  "auto_write_enabled": {
    "description": "开启凌晨自动写日记",
    "type": "bool",
    "default": true,
    "hint": "关闭时不会注册任何自动写作定时任务。"
  },
  "livingmemory_db_path": {
    "description": "LivingMemory 数据库路径（可选）",
    "type": "string",
    "default": "",
    "hint": "留空时使用 AstrBot 标准 plugin_data 下的默认位置。"
  },
  "generation_provider_id": {
    "description": "日记生成模型（可选）",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "留空时使用触发私聊对应的模型；v1.1 管理页的补写/重写必须显式配置此项。"
  },
  "website_sync_enabled": {
    "description": "启用网站镜像同步",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；同步失败不会影响本地日记。"
  },
  "website_sync_path": {
    "description": "网站日记目录",
    "type": "string",
    "default": "",
    "hint": "启用同步时必须显式填写；插件只写入该目录。"
  },
  "provider_retry_count": {
    "description": "生成失败重试次数",
    "type": "int",
    "default": 2,
    "hint": "不含首次调用；失败原因会保存到 generation_state.json。"
  },
  "inactive_minutes": {"description":"自动写作静默分钟数","type":"int","default":90},
  "fallback_inactive_minutes": {"description":"04:00兜底静默分钟数","type":"int","default":60},
  "cron_start_delay_minutes": {"description":"00:00 后开始检查的延迟分钟数","type":"int","default":30},
  "on_this_day_reminder_enabled": {
    "description": "主动提醒那年今日",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；仅授权用户的普通私聊首次触发，群聊和命令不会提示。"
  },
  "low_activity_round_threshold": {
    "description": "低活跃私聊轮数阈值",
    "type": "int",
    "default": 2,
    "hint": "当天授权私聊有效入站轮数不超过此值时使用 low_activity。"
  },
  "sparse_memory_threshold": {
    "description": "稀疏日记记忆阈值",
    "type": "int",
    "default": 2,
    "hint": "私聊轮数超过低活跃阈值后，当天 LivingMemory 不超过此值时使用 sparse。"
  },
  "recent_context_days": {
    "description": "近期私聊上下文天数",
    "type": "int",
    "default": 3
  },
  "historical_memory_min_count": {
    "description": "低活跃随机历史记忆最少条数",
    "type": "int",
    "default": 1
  },
  "historical_memory_max_count": {
    "description": "低活跃随机历史记忆最多条数",
    "type": "int",
    "default": 3
  },
  "reflection_cooldown_days": {
    "description": "随机历史记忆回忆冷却天数",
    "type": "int",
    "default": 30,
    "hint": "仅实际写入日记的历史记忆会进入软冷却。"
  }
}
